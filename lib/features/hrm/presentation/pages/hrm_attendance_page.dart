import 'package:flutter/material.dart';

/// Trang Chấm công của module HRM
class HrmAttendancePage extends StatelessWidget {
  const HrmAttendancePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Chấm công')),
      body: const Center(child: Text('Trang đang được phát triển')),
    );
  }
}
